language: c
dist: xenial
addons:
  apt:
    packages:
    - libffi-dev
    - gir1.2-glib-2.0
    - libgirepository1.0-dev
    - guile-2.0-dev
    - libtool
    - texinfo
script: autoreconf -Wall && ./configure && make && make check    
